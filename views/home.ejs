<%- include('partials/header') -%>

    <h1>Hello! I'm going to do some stuff</h1>
    <p><%= homeText %></p>
    <% posts.forEach((post) => { %>
        <h1><%= post.title %></h1>
        <p>
            <% console.log('This is post.title', post.title) %>
            <%= post.content.substring(0, 100) + '...' %>
            <a href="/post/<%= post.title %>">Read More</a>
        </p>
    <% }); %>
    
<%- include('partials/footer') -%>